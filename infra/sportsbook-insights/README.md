# Sportsbook Insights Infrastructure

Terraform configuration for PW-95 Sportsbook Insights AWS resources (Lambda, API Gateway).

This is currently scaffolding only — the provider is configured but no resources are defined yet, and this has not been applied. Phase 3 will add the Lambda function, API Gateway, and supporting IAM resources, plus a decision on remote state storage.
